\documentclass{article}
\usepackage{amsmath}

\begin{document}

Singular Value Decomposition (SVD). It . Given a matrix $A$, SVD factorizes it into the form:

\[
A = U \Sigma V^T
\]

where $U$ is an orthogonal matrix containing the left singular vectors, $\Sigma$ is a diagonal matrix containing the singular values, and $V$ is an orthogonal matrix containing the right singular vectors.

For example, consider the matrix:

\[
A = \begin{pmatrix}
3 & 1 & 4 \\
1 & 5 & 9 \\
2 & 6 & 5
\end{pmatrix}
\]

Performing SVD on $A$, we obtain:

\[
U = \begin{pmatrix}
-0.14 & -0.92 & -0.36 \\
-0.89 & -0.23 & 0.39 \\
-0.43 & 0.32 & -0.84
\end{pmatrix},
\Sigma = \begin{pmatrix}
12.3 & 0 & 0 \\
0 & 4.06 & 0 \\
0 & 0 & 1.02
\end{pmatrix},
V^T = \begin{pmatrix}
-0.30 & -0.54 & -0.78 \\
-0.81 & -0.25 & 0.52 \\
-0.50 & 0.80 & -0.35
\end{pmatrix}
\]

These matrices capture structure of the original matrix $A$, allowing for  analysis., and    reconstructions of data.

\end{document}
